Then(/^it should bind to default port (\d+)$/) do |port|
  pending
end

Then(/^it should serve HTTP requests$/) do
  pending
end

Then(/^it should serve content from directory "(.*?)"$/) do |dir|
  pending
end

